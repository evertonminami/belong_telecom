server:
  port: 8080
spring:
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:telecom
    username: sa
    password:
  h2:
    console:
      enabled: true